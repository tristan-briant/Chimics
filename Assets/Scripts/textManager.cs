using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class textManager : MonoBehaviour {
	
	void Update () {
        // Keep text with the horizontal orientation
        transform.rotation = Quaternion.identity;
    }
}
